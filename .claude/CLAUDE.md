# Dotfiles Project Instructions

## Testing Changes

After making changes to shell configuration files (nushell, powershell, tmux, etc.), always verify them by launching the relevant shell and testing the affected command or feature — unless the change is destructive (e.g., deletes files, kills processes, modifies system state). For non-destructive changes, run the shell and execute the command to confirm it works as expected before considering the task complete.
